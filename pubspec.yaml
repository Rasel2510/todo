name: todo
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.9.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  path_provider: ^2.1.5
  provider: ^6.1.5+1
  sqflite_common_ffi: ^2.3.3+1
  sqflite_common_ffi_web: ^0.4.3+3 
  path: ^1.9.0
  sqflite: ^2.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: "^0.13.1"

# SIMPLIFIED - NO ADAPTIVE ICON:
flutter_icons:
  android: true
  ios: true
  image_path: "assets/notee.png"

flutter:
  uses-material-design: true

  assets:
    - assets/notee.png